import React from 'react';

import { MainLayout } from 'layouts/MainLayout';
import { ReturnComponentType } from 'types';

const Index = (): ReturnComponentType => {
  return <MainLayout>Album page</MainLayout>;
};

export default Index;
