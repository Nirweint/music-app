import React from 'react';

import { MainLayout } from 'layouts/MainLayout';
import { ReturnComponentType } from 'types';

const Create = (): ReturnComponentType => {
  return (
    <MainLayout>
      <h1>Load new track</h1>
    </MainLayout>
  );
};

export default Create;
